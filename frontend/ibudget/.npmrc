# NPM Configuration for iBudget Frontend

# Use legacy peer dependency resolution to handle Angular 20 + ng2-charts compatibility
# ng2-charts@8.0.0 requires @angular/cdk@>=19.0.0 which tries to pull Angular 21,
# but the library works fine with Angular 20.3.15
legacy-peer-deps=true
