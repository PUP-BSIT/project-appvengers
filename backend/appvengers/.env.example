# iBudget Backend Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# --- Database Configuration ---
# Local Development (default - matches application.properties):
#   DB_URL=jdbc:mysql://localhost:3306/ibudget_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&autoReconnect=true
#   DB_USERNAME=ibudget_user
#   DB_PASSWORD=your_local_password
#
# Production VPS (Hostinger VPS at 72.61.114.163):
#   DB_URL=jdbc:mysql://localhost:3306/ibudget_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&autoReconnect=true
#   DB_USERNAME=ibudget_user
#   DB_PASSWORD=<production_password_on_vps>
#
DB_URL=jdbc:mysql://localhost:3306/ibudget_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&autoReconnect=true
DB_USERNAME=ibudget_user
DB_PASSWORD=your_database_password

# --- JWT Configuration ---
# CRITICAL: Use a cryptographically secure secret (MINIMUM 32 chars, RECOMMENDED 64+ chars)
# 
# Generate with one of these commands:
#   PowerShell (Windows): $bytes = New-Object byte[] 48; (New-Object Security.Cryptography.RNGCryptoServiceProvider).GetBytes($bytes); [Convert]::ToBase64String($bytes)
#   OpenSSL (Mac/Linux): openssl rand -base64 64
#
# IMPORTANT: NEVER reuse secrets across environments (dev/staging/prod)
# Example (64 characters - DO NOT USE THIS, generate your own):
JWT_SECRET=CHANGE_THIS_C8mSgxD2AGUCOFo2hRVToiwJyn4KoHwwvYg5m_hoTZCWlgp9ZbRRvMjxeOYmB9Tx

# --- Server Configuration ---
SERVER_PORT=8081

# --- Email Configuration ---
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587 # SSL/TLS port for Gmail
MAIL_USERNAME=your_smtp_username
MAIL_PASSWORD=your_smtp_password

# Verification link base URL (Sample change this to your deployed backend URL)
APP_VERIFICATION_BASE=http://123.456.789.0:8081/api/auth/verify-email?token=

# Password reset link base URL (change to your deployed frontend URL)
# Development: http://localhost:4200/reset-password?token=
# Production: https://i-budget.site/reset-password?token=
PASSWORD_RESET_FRONTEND_URL=http://localhost:4200/reset-password?token=

# Email address used as sender in outgoing emails
APP_EMAIL_FROM=your_email@example.com

# --- Google OAuth2 Configuration ---
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
