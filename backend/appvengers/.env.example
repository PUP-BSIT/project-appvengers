# iBudget Backend Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# --- Database Configuration ---
DB_URL=jdbc:mysql://YOUR_HOST:3306/YOUR_DATABASE?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&autoReconnect=true
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# --- JWT Configuration ---
# CRITICAL: Use a cryptographically secure secret (MINIMUM 32 chars, RECOMMENDED 64+ chars)
# 
# Generate with one of these commands:
#   PowerShell (Windows): $bytes = New-Object byte[] 48; (New-Object Security.Cryptography.RNGCryptoServiceProvider).GetBytes($bytes); [Convert]::ToBase64String($bytes)
#   OpenSSL (Mac/Linux): openssl rand -base64 48
#
# IMPORTANT: NEVER reuse secrets across environments (dev/staging/prod)
# Example (64 characters - DO NOT USE THIS, generate your own):
JWT_SECRET=CHANGE_THIS_C8mSgxD2AGUCOFo2hRVToiwJyn4KoHwwvYg5m_hoTZCWlgp9ZbRRvMjxeOYmB9Tx

# --- Server Configuration ---
SERVER_PORT=8081
